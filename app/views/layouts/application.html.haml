!!!
%html
  %head
    %title= page_title
    %meta{content: @meta_keywords, name: "keywords"}
    %meta{content: @meta_description, name: "description"}
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    #wrapper
      #header
        #logo
          %h1= link_to "Emtnest Embroidery Patterns and Designs", root_path
        #account
          = render 'shared/account_navigation'
        #navbar
          %ul
            %li= link_to "Home", root_path
            %li= link_to "Patterns", products_path
      #main
        #sidebar
          = form_tag root_path, method: 'get' do
            = text_field_tag :search, params[:search], placeholder: "Search Patterns"
            = submit_tag "Search", name: nil, class: 'btn'
          %ul
            - @categories.each do |category|
              %li= link_to category.name.titleize, '#'
        #content
          = yield
      #footer
        %ul
          %li= link_to "Home", "#"
          %li= link_to "Contact", "#"
        = link_to "EMTnest.com", root_path
        &copy; 2013
      